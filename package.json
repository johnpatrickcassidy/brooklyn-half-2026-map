{
  "name": "brooklyn-half-closures-map",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"const CONFIG = { GOOGLE_MAPS_API_KEY: '$GOOGLE_MAPS_API_KEY' };\" > config.js",
    "dev": "npx serve"
  }
}
